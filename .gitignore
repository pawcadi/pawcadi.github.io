# OS junk
.DS_Store
Thumbs.db

# Local Claude Code config (kept private — never publish)
.claude/

# Local agent skills/tooling (not part of the website)
.agents/
skills-lock.json

# Project context — SENSITIVE (supplier, costs, strategy). Never publish.
CLAUDE.md
